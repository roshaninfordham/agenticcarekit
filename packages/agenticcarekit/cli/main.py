"""Entry point for `ack` and `agenticcarekit`. Placeholder until W-G lands."""

from __future__ import annotations

import sys

from agenticcarekit import __version__


def main() -> None:
    print(f"agenticcarekit {__version__} — CLI under construction (Phase 0). No telemetry, ever.")
    sys.exit(0)


if __name__ == "__main__":
    main()
