Your previous response did not satisfy the required JSON Schema.

Schema:
{schema_json}

Original text:
{text}

Your previous (malformed) response:
{malformed}

Validation errors:
{errors}

Return a corrected, single JSON object that matches the schema exactly.
No prose, no markdown code fences, no commentary — JSON only.
